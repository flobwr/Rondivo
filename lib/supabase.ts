/**
 * Placeholder Supabase client — no project connected yet.
 *
 * Every data access in this app goes through `services/*.ts`, which today
 * reads/writes the mock arrays in `data/*.ts`. To connect a real Supabase
 * project:
 *   1. `npm install @supabase/supabase-js`
 *   2. Set EXPO_PUBLIC_SUPABASE_URL / EXPO_PUBLIC_SUPABASE_ANON_KEY (see .env.example)
 *   3. Uncomment the client below
 *   4. Rewrite the inside of each services/*.ts function to query `supabase`
 *      instead of the matching data/*.ts module — no screen needs to change,
 *      they only ever call services/*.ts.
 */

// import { createClient } from '@supabase/supabase-js';
//
// export const supabase = createClient(
//   process.env.EXPO_PUBLIC_SUPABASE_URL!,
//   process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!
// );

export {};
