/**
 * Living — Rondivo's signature transition.
 *
 * Public API. Screens import from here (or from `@/components/ui`), never
 * from the files inside.
 */

export { LivingCard } from './LivingCard';
export { LivingLayer, useLiving } from './LivingLayer';
export type { LivingHandle, LivingRect, LivingSource } from './types';
